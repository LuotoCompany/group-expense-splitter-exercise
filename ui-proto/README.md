
  # Expense Splitter App

  This is a code bundle for Expense Splitter App. The original project is available at https://www.figma.com/design/xr2izfTDaSVcSdcX8Pi4NG/Expense-Splitter-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  