# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=expense_splitter_dev

# Redis Configuration
# Leave empty for no password in development
REDIS_PASSWORD=

# Connection URLs (these are auto-constructed, but you can override)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/expense_splitter_dev
# REDIS_URL=redis://:@localhost:6379
